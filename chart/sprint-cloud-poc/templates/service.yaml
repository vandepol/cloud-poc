apiVersion: v1
kind: Service
metadata:
  name: {{ template "sprint-cloud-poc.fullname" . }}
  labels:
    app: {{ template "sprint-cloud-poc.name" . }}
    chart: {{ template "sprint-cloud-poc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "sprint-cloud-poc.name" . }}
    release: {{ .Release.Name }}
